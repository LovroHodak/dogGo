<a href="/logout" class="btn btn-secondary" id="transparent-btn">Logout</a>
<a href="/volunteer" class="btn btn-secondary" id="transparent-btn"> Back to the dashboard ◀ </a>
<a href="/volunteer/search" class="btn btn-secondary" id="transparent-btn"> Back to search ◀ </a>
<hr>

<div class="card mb-3" style="max-width: 8000px;">
  <div class="row no-gutters">
    <div class="col-md-4">
      <img src="{{foundDoggo.imageUrl}}" onerror="this.src='/images/img_blank.jpg';" class="card-img"
        alt="Doggo profile">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h2 class="card-title">Meet dog, {{foundDoggo.name}}!</h2>
        <hr>
        <p class="card-text">
        <h5>
          <br>
          Breed: {{ foundDoggo.breed }}
          <br>
          Age: {{ foundDoggo.age}}
          <br>
          Size: {{ foundDoggo.size }}
          <br>
          Description: {{ foundDoggo.description }}
          <br>
          City: {{ capitalizedCityOnDogProfile }}
          <br>
        </h5>
        <p class="card-text">
          <small class="text-muted">
            {{#if foundDoggo.walkies}}
            <p>Available for walkies!</p>
            {{/if}}
            {{#if foundDoggo.foster}}
            <p>Available for fostering!</p>
            {{/if}}

          </small>
        </p>

        <div class="secondary-nav">
          <a href="/volunteer/{{foundDoggo._id}}/messages" class="btn btn-primary">Message</a>
        </div>
      </div>
    </div>
  </div>
</div>